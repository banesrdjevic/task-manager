{
  "name": "task-api-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --verbose"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000,
    "watchman": false
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
